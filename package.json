{
	"packageManager": "pnpm@11.5.1",
	"scripts": {
		"build:contract": "pnpm --filter @trpg/api-contract build",
		"build:engine": "pnpm --filter @trpg/sheet-engine build",
		"build:projection": "pnpm --filter @trpg/sheet-projection build",
		"check:contract-stack": "pnpm --filter @trpg/api-contract run build && pnpm --filter @trpg/api-contract run test && pnpm --filter trpg-server run build && pnpm --filter trpg-server run typecheck:test && pnpm --filter trpg-server run test -- --testPathPattern \"api-response|response.interceptor|http-exception.filter|auth.controller|user.controller|user-output.contract|character-wire.contract|character.controller.http\" && pnpm --filter trpg-next-app run typecheck && pnpm --filter trpg-next-app run lint && pnpm --filter trpg-next-app run test",
		"static:deps": "node tools/static-analysis/run.js dependencies",
		"static:duplication": "node tools/static-analysis/run.js duplication",
		"static:independence": "node tools/static-analysis/run.js independence",
		"test:contract": "pnpm --filter @trpg/api-contract test",
		"test:engine": "pnpm --filter @trpg/sheet-engine test",
		"test:projection": "pnpm --filter @trpg/sheet-projection test"
	},
	"devDependencies": {
		"@types/mocha": "^10.0.10"
	},
	"dependencies": {
		"@types/jest": "^30.0.0",
		"nest-winston": "^1.10.2",
		"winston": "^3.17.0"
	}
}
