{
  "name": "@trpg/sheet-engine",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "bench:annotation": "pnpm run build && node scripts/annotation-bench.ts",
    "bench:h18": "pnpm run build && node scripts/h18-bench.ts",
    "build": "tsc -p tsconfig.json",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "zod": "4.1.11"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "jest": "29.7.0",
    "ts-jest": "29.4.4",
    "typescript": "5.9.2"
  }
}
